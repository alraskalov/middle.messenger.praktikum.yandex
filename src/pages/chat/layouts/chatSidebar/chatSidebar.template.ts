const template = `
    <div class="chat-sidebar__header">
        
        {{{ buttons }}}
        {{{ search }}}
    </div>

    <div class="chat-sidebar__chat-list">
        {{{ chatList }}}
    </div>
`;

export default template;
